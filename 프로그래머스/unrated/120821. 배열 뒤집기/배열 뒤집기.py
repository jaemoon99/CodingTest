def solution(num_list):
    # num_list_len = len(num_list)
    # answer = [0]*num_list_len
    # for x in num_list:
    #     answer[num_list_len-1] = x
    #     num_list_len -= 1  
    return num_list[::-1]